## .env.example
# Porta del server
PORT=3000

# URL del database PostgreSQL (Railway fornisce automaticamente questa variabile)
DATABASE_URL=postgresql://user:password@host:port/database

# URL client (facoltativo, se serve CORS)
CLIENT_URL=http://localhost:5173
